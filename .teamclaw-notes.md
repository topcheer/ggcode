# Project Notes

- Architecture docs and interface definitions created 2026-04-03
- `internal/context` package uses import alias `ctxpkg` in consuming files
- Session store avoids circular dep with cost package by storing cost as `[]byte` JSON
- `internal/cost/types.go` defines `TokenUsage` locally to avoid importing provider package (circular dep prevention)
- All interface files are Go source but NOT part of a compilable module yet (no go.mod) — they're design artifacts for Phase 1 implementation
