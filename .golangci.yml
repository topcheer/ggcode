version: "2"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - gofmt
    - unused

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - govet
