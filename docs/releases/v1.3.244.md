# v1.3.244

发版日期：2026-09-20

v1.3.243 以来 36 commits，关闭 20+ 个 issue（#2582-#2598、#2601-#2617、#2622、#2625、#2626）。

## Provider / 认证层

- **OpenCode OAuth 设备流登录 + Zen 模型发现**（新功能）：opencode vendor 支持完整 device flow 认证与模型列表发现（#2592）
- **#2602**：opencode login 慢速轮询 `slow_down` 未按 RFC 8628 退避——补齐指数退避
- **#2622**：过期的 OAuth token 无 refresh token 时静默解析成功——改为响亮失败，杜绝用死 token 发请求

## 桌面自动化工具（iterm2/kitty/ghostty/iOS，批量修复）

- **#2593/#2598**：iterm2 broadcast 定位真实菜单树（原路径失效）；kitty send-text 支持 F 键与 `--` 参数分隔
- **#2594/#2597**：iterm2 输入固定换行 NO（此前每键多敲一次回车）；kitty action 检查 focus 错误
- **#2625**：iterm2 send_key 拒绝 ctrl+非字母组合（终端协议不支持的组合原样放行导致乱码）
- **#2589**：ghostty split 在命令为空时才 cd-only——不再丢弃 working_dir
- **#2587**：menu_select 遵守 schema 的 app 参数（darwin）；配套测试加 darwin build tag 门
- **#2588**：信号击杀转换为 128+N 退出码，exit-code 诊断不再漏报
- **#2583/#2586**：set_window_bounds 描述披露窗口定位语义
- **#2582/#2584**：Windows quit_app 空目标守卫；compound 预检与执行对齐
- **#2604/#2605**：iOS listApps 剥离 plist 引号；type/press 转义反斜杠
- **#2603**：LSP 工具族导出 WorkingDir 并实现 Cloner——sub-agent 快照不再丢工作目录

## 工具语义与审查

- **#2590**：revspec 参数按 revspec 语义校验（原用 ref-name 规则误拒合法 range）
- **#2595/#2596**：KnowledgeGraphTool 实现 Clone；kg link 的 ValidFrom 默认 now
- **#2606**：review_changes 不再把 `--- a/` diff 头计为删除行
- **#2607**：剔除死 sort -V shell 兼容模式及其偏差测试
- **staging quality**：生成文件模式改锚定匹配，不再误配子串

## TUI

- **#2617**：排队图片恢复改为 append 合并——不再覆盖运行中粘贴的图片
- **#2616**：knight 任务取消注册表在 NewModel 预建（原懒建在取消路径上 panic）
- **#2615**：parseIntPositive 拒绝后缀乘法溢出（`"10x10"` 类输入）
- **#2610**：IM 编辑框字段保存后光标位置保持
- **#2609**：ask_user 自由备注可清除；远端答案仍受保护
- **#2626**：nostr 面板先规范化 nsec 再派生显示——修复成功后 npub/QR 空白

## Agent 行为（安全向）

- **MCP tool-search 改为 opt-in**：默认全量下发工具 schema，仅显式配置启用延迟搜索——消除静默降级（`2ec17b06`/`de355406`）

## 重构

- **#2612**：browser Execute 改 action handler 表分发
- **#2614**：sync-model-caps main 拆相位函数
