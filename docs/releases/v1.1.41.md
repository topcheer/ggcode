# ggcode v1.1.41

## Fixes

- **Windows CI smoke test: fix Start-Process redirect conflict** — Use unique temp files for stdout/stderr redirects instead of `NUL` for both, which PowerShell rejects as duplicate targets.

## Compare

- Full diff: https://github.com/topcheer/ggcode/compare/v1.1.40...v1.1.41
