# v1.3.104

## Fixes

- **Flaky `TestClaudeCallback_CodeExchange` test**: The test had an unbounded `<-flow.callbackCh` channel receive that could block forever under CI load (30s timeout). Both callback test cases now use a `select` with a 5-second timeout, failing fast instead of hanging indefinitely.
