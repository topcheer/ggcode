# v1.3.120

## Fixes

- **LSP notificationHandler data race**: Fixed a data race where the `readLoop` goroutine read `c.notificationHandler` while `sessionManager.acquire()` wrote it after `startClient()` had already launched the goroutine. Both read and write are now protected by `c.mu`.
