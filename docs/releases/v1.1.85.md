# ggcode v1.1.85

## Highlights

- **Stream emoji rendering** — All emoji and Unicode characters now render correctly in live stream frames. The stream renderer automatically replaces bitmap emoji (🔴🔧🎯⏳ etc.) with DejaVu Mono–compatible outline equivalents, preserving column alignment with 1-char padding.
- **Hardware encoder auto-detection** — On macOS (Apple Silicon/Intel), the stream encoder now auto-detects and uses `h264_videotoolbox` instead of software `libx264`, reducing CPU usage by ~4.5× on Mac Studio. Linux VA-API, NVIDIA NVENC, and AMD AMF are also probed automatically.
- **Knight panel skill deletion** — Skills in the Knight panel can now be deleted with `[d]` key. Staging skills with duplicate names are handled correctly via file-path matching.
- **IM `/stream` remote command** — IM inbound messages in TUI mode now support `/stream start|stop|status|config` for live stream control.
- **IM `/restart` fix in TUI mode** — Remote `/restart` via IM no longer causes the application to exit without restarting.

## Fixes and improvements

- **Plan mode restore**: `RememberMode()` now reads from `policy.CurrentMode()` instead of stale model copy, fixing plan mode not restoring to the previous mode after exit.
- **Stream emoji width alignment**: `runeWidth()` now correctly returns 2 for emoji presentation characters in U+2300–U+23FF (⏳⌛⏰), U+2700–U+27BF (✅❌❗❓), U+2B50–U+2B55 (⭐⭕), U+1F7E0–U+1F7FF (colored circles), and U+2600–U+26FF (⚠ etc.). Emoji replacements are padded with a trailing space to preserve column alignment in bordered frames.
- **Braille spinner**: Spinner characters (⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏) map to ◐◓◑◒ circle quadrants for stream rendering. Tick interval adjusted from 80ms to 150ms to maintain smooth rotation speed with 4 frames instead of 10.
- **Knight panel**: Left column width increased from 18 to 24 to prevent ugly line wrapping of section labels.
- **Knight staging**: Approve/reject staging skills now use file-path matching (`PromoteStagingByPath`/`RejectStagingByPath`) instead of name-only, fixing ambiguity with duplicate skill names across scopes.
- **Remote restart**: `scheduleRemoteRestart` now uses a dedicated `remoteRestartMsg` handled in the Update loop, correctly setting `restartRequested` under Bubble Tea's value semantics. Previously the tick callback modified a stale model copy, causing `finalModel.restartRequested` to be false after `tea.Quit`.
- **VideoToolbox quality**: Hardware encoder on macOS uses `-profile high -quality realtime` for low-latency streaming.

## Compare

https://github.com/topcheer/ggcode/compare/v1.1.84...v1.1.85
