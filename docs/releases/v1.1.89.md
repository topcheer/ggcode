# ggcode v1.1.89

## Highlights

- **IM tool result display aligned with TUI/daemon** — Telegram, Discord, Slack, Feishu, QQ, DingTalk now show the same concise tool summaries as the terminal

## IM tool result changes

### Header + summary body
- **write_file** — `📝 filename (42 lines)` with line count
- **edit_file** — `✏️ filename (+5 -3)` with diff counts
- **search_files / grep / glob** — `🔍 pattern — 12 matches` with match count
- **git_status** — `🔧 Git Status` with modified/added/deleted/untracked counts
- **git_diff** — `🔧 Git Diff (+15 -8)` with diff counts
- **git_log** — `🔧 Git Log` with up to 3 recent commits
- **spawn_agent** — `🤖 Starting subagent fix the bug` with task description
- **run_command** — `✅ ls` on success, `❌ bad_cmd` + error on failure

### Completely hidden (no message sent)
- Background command lifecycle: `read_command_output`, `wait_command`, `stop_command`, `write_command_input`, `list_commands`
- Secondary git: `git_show`, `git_blame`, `git_branch_list`, `git_remote`, `git_stash_list`, `git_add`, `git_commit`, `git_stash`
- Agent management: `wait_agent`, `list_agents`
- All LSP tools: `lsp_*`
- `list_directory`, `web_fetch`, `web_search`, `skill`, `save_memory`

## Other improvements

- Add release checklist to GGCODE.md to prevent missing steps in future releases

## Upgrade notes

- No breaking config changes
- IM tool result styling is automatic across all adapters

## Compare

- Full diff: https://github.com/topcheer/ggcode/compare/v1.1.88...v1.1.89
